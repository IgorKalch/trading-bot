# СТРАТЕГІЯ ORB (Opening Range Breakout) — DAX / GER40

> **Джерело правди.** Цей файл — єдиний опис логіки стратегії. Той самий алгоритм
> виконується у production-торгівлі (`src/tradingbot/strategy/orb.py` + `management/trade_manager.py`),
> у бектест-движку та в майбутніх оптимізаціях. **Жодне правило стратегії не може
> бути захардкоджене в коді без опису в цьому файлі.** Кожне правило має посилання
> на параметр конфігурації (`config/config.yaml`), яким воно керується.
>
> Першоджерела: методичка «FDAX M5 ORB» (Hnurewnik & Coba), відео «Setup Opening
> Range» (транскрипт), веб-дослідження фільтрів ORB (Zarattini & Aziz SSRN 4416622 /
> 4729284, TradingStats ES/NQ 2014–2026 та ін.), офіційні правила Funding Pips
> (help.fundingpips.com, зафіксовано 2026-07-06).

---

## 0. Загальна ідея

Перші хвилини після відкриття основної сесії біржі формують діапазон (Opening
Range, OR). Пробій цього діапазону з якісним закріпленням дає статистичну перевагу
продовження руху в бік пробою. Стратегія торгує **максимум 2 позиції на день**:

1. **Перша позиція** — пробій OR **у напрямку овернайт-гепу** (продовження сили
   нічної сесії).
2. **Друга позиція** — **реверсивний пробій** протилежної межі OR (незалежно від
   того, чи була відкрита перша).

Інструмент: **GER40/DE40 (CFD на DAX)** у MT5. Статистика першоджерела зібрана на
FDAX (ф'ючерс), логіка переноситься на CFD 1:1 у пунктах (1 lot CFD = €1/пункт;
«pips» у першоджерелі = пункти індексу).

Базова статистика першоджерела (240 торгових днів, 06.2024–06.2025, без комісій):

| Модель | Позицій | WinRate | PF | Річний PnL (ризик 1%) | Макс. серія стопів |
|---|---|---|---|---|---|
| 1R (обидві позиції) | 275 | 59.9% | 1.51 | 55% | ~6 |
| 1.5R | 275 | 46.9% | 1.32 | 57.5% | ~9 |
| 2R | 275 | 37.2% | 1.19 | 34% | ~12 |

Висновок першоджерела: **1R — найстабільніша модель**, 1.5R — максимум прибутку,
2R — не використовувати (від'ємний запас переваги). Проміжні 1.25R / 1.75R —
кандидати для бектесту.

---

## 1. Логіка входу в позицію (повний алгоритм)

```
Для кожного торгового дня:
 1. О 09:00 Europe/Berlin (10:00 Київ) починається сесія Xetra.
 2. Свічки OR-вікна (09:00–09:05, перша M5-свічка) формують OR: high та low.
 3. Обчислюється геп: open(09:00) − close(попередньої сесії 17:30 Berlin).
 4. Застосовуються денні фільтри (§7). Якщо день відфільтровано — жодних угод,
    у Telegram надсилається причина пропуску.
 5. Після 09:05 кожна нова ЗАКРИТА M5-свічка перевіряється на пробій:
    a. Перший пробій будь-якої межі OR (закриття за межею) = кандидат «перша позиція».
       - напрямок має збігатися з напрямком гепу (§4), інакше пропуск
         (межа вважається використаною);
       - підтвердження якості свічки (§5);
       - вікно входу: до 11:00 Berlin (§8).
    b. Наступний підтверджений пробій ПРОТИЛЕЖНОЇ межі OR = кандидат «друга позиція»
       (реверс). Без геп-фільтра. Вікно: до 17:00 Berlin. Кожна межа OR може дати
       сигнал лише один раз; максимум 2 входи на день.
 6. Вхід — ринковим ордером на ВІДКРИТТІ НАСТУПНОЇ свічки після свічки-підтвердження.
    (Входи на ретесті першоджерело протестувало й відхилило.)
 7. SL і TP розраховуються за §9–§10; обсяг — за §13.
 8. Перед відправленням ордера виконуються guard-перевірки Funding Pips (§14).
    Якщо guard забороняє — угода пропускається з нотифікацією.
```

Якщо перша позиція ще відкрита, а підтвердився реверсивний пробій — перша позиція
**закривається ринком** перед відкриттям другої (`entry.close_open_position_on_reversal`,
за замовчуванням `true`).

## 2. Визначення ORB-зони

| Правило | Значення | Параметр конфігу |
|---|---|---|
| Якір сесії | Відкриття Xetra 09:00 Europe/Berlin (= 10:00 Київ цілий рік) | `session.timezone`, `session.open` |
| Тривалість OR | Перші **5 хвилин** (одна M5-свічка) | `session.or_minutes: 5` |
| OR.high / OR.low | High/low усіх свічок OR-вікна | — |
| Таймфрейм | M5 | `strategy.timeframe: M5` |

Важливо для MT5: час сервера брокера ≠ час біржі. Бот конвертує все в UTC і
обчислює 09:00 Berlin на кожен день окремо (`mt5.server_timezone`, режим
`eet_us_dst` — стандартна конвенція «GMT+2 взимку / GMT+3 при літньому часі США»).
У тижні розбіжності DST США/ЄС (березень, кінець жовтня) фіксована «10:00 за
сервером» зсувається — тому якір завжди рахується від Europe/Berlin.

## 3. Правила входу

1. **Пробій** = M5-свічка **закрилася** за межею OR (`close > OR.high` для лонга,
   `close < OR.low` для шорта).
2. Пробій має пройти підтвердження (§5) і фільтри (§7).
3. **Виконання**: ринковий ордер одразу після закриття свічки-підтвердження
   (= відкриття наступної свічки). TP перераховується від фактичної ціни виконання,
   щоб RR був точним.
4. Кожна межа OR може згенерувати **лише один** сигнал за день (навіть якщо його
   пропущено фільтром — базова модель торгує тільки перший пробій кожної межі).
5. Перший підтверджений пробій дня = «перша позиція», підтверджений пробій
   протилежної межі після нього = «друга позиція» (реверс). Третьої не буває
   (`entry.max_positions_per_day: 2`).

## 4. Логіка гепу (перша позиція)

Геп = `open(09:00 Berlin) − close(17:30 Berlin попереднього торгового дня)`
(канонічне визначення овернайту DAX; **не** використовувати закриття добової свічки
MT5 — вона закривається о 23:00/24:00 Berlin).

| Правило | Дефолт | Параметр |
|---|---|---|
| Перша позиція тільки в напрямку гепу (навіть мінімального) | `true` | `entry.first_in_gap_direction_only` |
| Контр-геп вхід (у бік перекриття гепу) дозволений лише якщо \|геп\| ≤ X пунктів | `0` (вимкнено) | `entry.counter_gap_max_points` (першоджерело: ~60) |
| Пропуск дня при завеликому гепі | `0` (вимкнено) | `filters.max_gap_points` |

Обґрунтування (транскрипт): пробій у бік перекриття гепу має «дуже поганий winrate»;
при широкому гепі ціна типово перекриває ~0.5 діапазону гепу і розвертається,
вибиваючи стоп. На 240 днях: 186 днів пробій ішов у бік гепу, 54 — проти.

Якщо `prev_session_close` недоступний (перший день даних) — геп-фільтр не
застосовується, працює як «геп відсутній»: перша позиція в будь-який бік.
**Нульовий геп** (open == prev_close) напрямку не має — правило неактивне,
дозволені обидва боки. Референс попереднього закриття береться лише з барів
у межах касової сесії відповідного дня (вечірні CFD-бари після 17:30 і дні
біржових свят ігноруються).

## 5. Підтвердження входу («закріплення»)

| Умова | Дефолт | Параметр |
|---|---|---|
| Закриття свічки за межею OR — обов'язково | завжди | — |
| Свічка направлена (тіло в бік пробою: для лонга close > open) | `true` | `confirmation.require_body_close` |
| Імпульсність: тіло ≥ 50% діапазону свічки | `0.5` | `confirmation.min_body_to_range` |
| Мінімальний вихід за межу: max(X пунктів, Y·ширини OR) | `0` / `0` (вимкнено) | `confirmation.min_break_points`, `confirmation.min_break_or_frac` |
| «Нема куди ставити стоп»: закриття далі ніж Z·ширини OR за межею → пропуск | `0` (вимкнено) | `entry.max_entry_distance_or_mult` |

Першоджерело: «закріплення має бути тілом, пробійна свічка достатньо імпульсна;
мікро-закріплення на міліметр не беремо». Пороги 0 за замовчуванням відтворюють
базову статистичну модель; кількісні значення — предмет оптимізації (§15).

## 6. Друга позиція (реверсивний пробій)

1. Розглядається **незалежно від того, чи була відкрита перша** (пробій міг бути
   пропущений геп-фільтром) — але тільки **після першого пробою дня**.
2. Тригер: підтверджене закріплення за **протилежною** межею OR.
3. Ті самі правила підтвердження (§5), SL (§9), TP (§10). Геп-фільтр не застосовується.
4. Вікно входу: до 17:00 Berlin (18:00 Київ) — `session.pos2_cutoff`.
5. Вимкнути другу позицію: `entry.allow_second_position: false`.

Статистика першоджерела: друга позиція була в ~37% днів (89 із 240), 1R WinRate
61.8%, PF 1.62, макс. серія 4 — стабільніша за першу. Обґрунтування: провалений
пробій відкриття часто розвертає день у тренд до закриття (Open Rejection Reverse);
дослідження ES/NQ підтверджують — на днях подвійного пробою другий пробій виграє
у 55–72% випадків.

## 7. Фільтри

Усі фільтри конфігуруються; `0`/`false` = вимкнено. Базова модель першоджерела —
без фільтрів (крім гепу §4 і вікон §8). Додаткові — з веб-дослідження, для бектесту.

| # | Фільтр | Дефолт | Параметр | Докази |
|---|---|---|---|---|
| 7.1 | Новини: не входити ±5 хв від high-impact подій EUR/USD (ForexFactory). Вікно перевіряється на **момент входу** (закриття свічки-підтвердження) | `true` | `filters.news_filter_enabled`, `news_buffer_before_min/after_min`, `news_currencies`, `news_min_impact` | Правило Funding Pips + практика; першоджерело: «пропуск новинних днів — потребує тестів» |
| 7.2 | Ширина OR: min/max у пунктах або ≤ k·ATR(D1) | `0` (вимк.) | `filters.min_or_width_points`, `max_or_width_points`, `max_or_width_atr_mult`, `atr_period` | Першоджерело закономірності НЕ знайшло (avg 66 п.); ком'юніті-бектест: skip при OR > 70 п.; US-дослідження: кап ~0.55% ціни |
| 7.3 | Максимальний геп (пропуск дня) | `0` (вимк.) | `filters.max_gap_points` | Похідне від §4 |
| 7.4 | Відносний обсяг OR-вікна (RVOL ≥ x від середнього за N днів) | `0` (вимк.) | `filters.min_or_rvol`, `rvol_lookback_days` | Найсильніший задокументований фільтр ORB (SSRN 4729284: прибутковість лише при RVOL > ~1.0) |
| 7.5 | Дні тижня (пропуск) | `[]` | `filters.skip_weekdays` | Ком'юніті: п'ятниця найгірша; US-дані: середа найслабша. Перевірити бектестом на DAX |
| 7.6 | VWAP: лонг лише вище сесійного VWAP, шорт — нижче. **ІНЕРТНИЙ** — за 258 сесій не відхилив жодного входу (Додаток Б) | `false` | `filters.vwap_filter` | SSRN 4631351 (непрямі докази) |
| 7.7 | Напрямок OR-свічки має збігатися з напрямком пробою | `false` | `filters.or_direction_filter` | TradingStats: предикція першого пробою 77–80% на ES/NQ |
| 7.8 | Максимальний спред на вході (live) | `6` п. | `filters.max_spread_points` | Захист від розширеного спреду на відкритті/новинах |
| 7.9 | Трендовий фільтр: вхід лише з боку MA(N) торгового таймфрейму, порахованої **до відкриття** сесії; опційно ще й нахил самої MA | `0` (вимк.) | `filters.trend_ma_period`, `trend_ma_require_slope` | Tom Hougaard, «Trade Mentor — Trading Strategy»: 89-періодна MA визначає тренд, проти неї він не торгує |
| 7.10 | Обсяг пробійної свічки ≥ k × середнього за останні N барів («high volume extension») | `0` (вимк.) | `filters.min_break_bar_rvol`, `break_rvol_lookback_bars` | tomtrades, «Watch Me Backtest My 71% Win Rate Strategy» |

**Про §7.6 (VWAP).** Фільтр реалізовано (`strategy/orb.py`), але на цих даних він
не зв'язує: за 258 сесій движок не записав жодного скіпу `vwap`. Причина
структурна — підтверджений пробій за межу OR майже завжди опиняється з
правильного боку сесійного VWAP, який сам здебільшого сформований цим же
діапазоном. Вмикати його немає сенсу, доки не зміниться означення (наприклад,
VWAP попереднього дня замість поточної сесії).

**Застереження від оверфітингу** (options.cafe ablation): кожен доданий фільтр
має бути обґрунтований бектестом окремо; «kitchen sink» із усіх фільтрів одразу
погіршував результат удвічі. Вмикати по одному.

## 8. Часові вікна (усі — Europe/Berlin)

| Подія | Час | Київ | Параметр |
|---|---|---|---|
| Відкриття сесії / початок OR | 09:00 | 10:00 | `session.open` |
| Кінець OR-вікна | 09:05 | 10:05 | `session.or_minutes` |
| Дедлайн входу першої позиції | 11:00 | 12:00 | `session.pos1_cutoff` |
| Дедлайн входу другої позиції | 17:00 | 18:00 | `session.pos2_cutoff` |
| Примусове закриття всіх позицій | 17:20 | 18:20 | `session.flat_time` |
| Закриття сесії (референс гепу) | 17:30 | 18:30 | `session.close` |

Дедлайн застосовується до **часу закриття свічки-підтвердження**. Першоджерело
тримало позиції до SL/TP без тайм-ауту; `flat_time` доданий як вимога
інтрадей-режиму та захист від нічних свопів і вікенд-правил пропа. Вплив
перевіряється бектестом (для fixed 1R TP типово досягається задовго до 17:20).

## 9. Stop Loss

- **Базове правило:** SL за **всім діапазоном OR** — протилежна межа ± буфер:
  лонг → `OR.low − sl_buffer_points`; шорт → `OR.high + sl_buffer_points`.
  Параметри: `stops.sl_buffer_points` (дефолт 2 п.).
- `stops.max_sl_points` (0 = вимкнено) — пропуск угоди, якщо стоп завеликий.
- 1R (ризик у пунктах) = |ціна_виконання − SL|.
- Альтернатива «стоп за свінгом усередині OR» першоджерелом протестована й
  **відхилена** (вплив на WinRate 1–3%, мала вибірка) — не реалізована; можлива
  як майбутній експеримент.

## 10. Take Profit і логіка RR

Два режими (`targets.tp_mode`):

1. **`fixed_rr`** (дефолт) — статичний TP на `targets.fixed_rr` × 1R від ціни
   виконання. Дефолт **1.0** (модель 1R — найстабільніша). Рекомендований діапазон
   1.0–1.5; **> 2R заборонено** першоджерелом (від'ємне матсподівання).
   TP ставиться в ордер (server-side у MT5).
2. **`trailing`** — без статичного TP, стоп підтягується кроками R (§11), вихід
   по стопу або о `flat_time`. Режим натхнений академічними результатами
   (tight stop + «дай прибутку бігти» перемагає фіксовані цілі на US-даних),
   **не підтверджений** статистикою першоджерела на DAX — тільки через бектест.

## 11. Trailing Stop (режим `trailing`)

Параметри: `targets.trail_start_r` (дефолт 0.5), `targets.trail_step_r` (дефолт 0.5),
`targets.breakeven_buffer_points` (дефолт 0).

```
R = |entry − initial_SL|
Коли профіт досягає (trail_start_r + k·trail_step_r)·R, k = 0,1,2,…:
    новий SL = entry + k·trail_step_r·R  (для шорта дзеркально)
k=0 → беззбиток (+ breakeven_buffer_points, якщо задано)
```

З дефолтами 0.5/0.5: при +0.5R → SL у беззбиток; +1.0R → SL на +0.5R;
+1.5R → SL на +1.0R і т.д. Стоп рухається **тільки в бік прибутку**; перевірка —
кожен тік-пол (live, `bot.poll_seconds`) або кожен бар M5 (backtest, консервативно).
Модифікація — `TRADE_ACTION_SLTP` (трейлінг MT5 клієнтський, бот веде його сам).

## 12. Керування відкритою позицією та умови виходу

Пріоритет правил (виконує `management/trade_manager.py`, спільний для live/backtest):

1. **SL / TP брокера** — виконуються server-side (детектуються через sync).
2. **Аварійне закриття** — еквіті наблизилось до буферного денного/загального
   ліміту пропа (§14) → закрити все ринком, стоп торгівлі на день.
3. **Pre-news flatten** (live, `prop.flatten_before_news`) — позиції закриваються
   ДО початку новинного вікна ±5 хв, щоб жодне закриття не потрапило всередину
   вікна (правило Master-рахунку).
4. **`flat_time`** (17:20 Berlin) — примусове закриття всіх позицій ринком.
5. **Реверс** — підтверджена друга позиція закриває першу (якщо ще відкрита);
   у live закриття виконується лише ПІСЛЯ проходження всіх перевірок нового
   входу (новини/спред/обсяг/guard) — інакше сигнал ігнорується цілком.
6. **Trailing** (§11, тільки в режимі `trailing`).

Часткові фіксації не використовуються (першоджерело тестувало бінарні виходи).

**Рестарти:** сигнал виконується лише «свіжим» — якщо з моменту закриття
свічки-підтвердження минуло понад ~90 с (рестарт бота, пізній старт), сигнал
використовується тільки для відбудови стану дня і НЕ виконується за поточною
ціною. Кожна межа OR лишається використаною.

## 13. Risk Management

| Правило | Дефолт | Параметр |
|---|---|---|
| Ризик на угоду, % від балансу | **1.0%** | `risk.risk_per_trade_pct` |
| Обсяг: `lots = ризик_$ / (SL_дистанція/tick_size · tick_value)`, округлення **вниз** до `volume_step` | — | `risk/position_sizing.py` |
| Якщо розрахований лот < мінімального — пропуск (або мін. лот) | пропуск | `risk.min_volume_fallback` |
| Кап обсягу (менше з двох: 20 лотів/клік у фірми та `symbol_info.volume_max`, на SIM1 = 10) | 10 | `risk.max_volume_lots` |

Калібрування ризику від макс. серії збитків (першоджерело, під 10% max DD пропа):

| Модель | Макс. серія | Безпечний ризик/угоду | Річний PnL |
|---|---|---|---|
| 1R | ~6 | **1.6%** (1.5% з комісіями) | ~88% |
| 1.5R | ~9 | **1.1%** | ~63% |
| 2R | ~12 | 0.8% | не варто |

Дефолт 1.0% консервативніший за розрахунковий максимум — рекомендовано підняти
до 1.5–1.6% лише після власного бектесту та на фазі Evaluation з урахуванням
актуальних лімітів моделі рахунку (§14: у 1-Step/Pro/Zero денний ліміт 3%, там
1.6% ризику = 2 стопи в день впритул до ліміту!).

## 14. Обмеження Funding Pips (guard-модуль `risk/prop_guard.py`)

Зафіксовано з офіційного help.fundingpips.com станом на 2026-07-06. Числа залежать
від моделі рахунку — **звір свою модель** і онови конфіг:

| Ліміт | 2-Step Standard | 1-Step | Pro | Flex | Zero | Параметр |
|---|---|---|---|---|---|---|
| Денний збиток | 5% | 3% | 3% | 4% | 3% | `prop.daily_loss_limit_pct` |
| Макс. просадка (статична від старту) | 10% | 6% | 6% | 12% | 5% trailing* | `prop.max_drawdown_pct` |

\* Trailing-режим Zero НЕ реалізований у guard — для Zero потрібна доробка.

Механіка (реалізована):
- Якір дня = **max(баланс, еквіті) на початку дня**; порушення рахується по
  **еквіті в реальному часі** (враховує floating PnL). День guard'а рахується
  за **платформним часом UTC+3** (як у FP), незалежно від сесійного дня
  стратегії. Якорі персистяться на диск атомарно; пошкоджений файл стану =
  відмова торгувати (fail closed), а не тихе перестворення якорів.
- **Буфери безпеки**: бот зупиняється на `limit − buffer`
  (`prop.daily_loss_buffer_pct`, дефолт 1.5 → стоп на −3.5% при ліміті 5%;
  `prop.max_drawdown_buffer_pct`, дефолт 2.0). Перед входом перевіряється
  worst-case: `еквіті − ризик_угоди` не має пробити буферний рівень.
- **Новини (Master):** заборонено відкривати/закривати позицію ±5 хв навколо
  red-folder події відповідної валюти (джерело — ForexFactory, як у FP). Guard
  блокує входи у вікні (`prop.restrict_news_trading`, `news_window_before/after_min`),
  а `prop.flatten_before_news` закриває відкриті позиції ДО початку вікна, щоб
  і закриття не потрапило всередину. Якщо календар новин недоступний, бот
  попереджає в Telegram; `prop.require_news_data: true` повністю блокує входи
  без даних календаря. На Evaluation обмеження немає — але «purposely trading
  news» заборонено, тому дефолт: фільтр увімкнено всюди.
- **Вікенд:** на Master (тимчасово, з 29.01.2026) позиції автозакриваються в
  п'ятницю. Стратегія інтрадей + `flat_time` → позицій на ніч/вікенд немає;
  додатковий guard блокує пізні п'ятничні входи (`prop.forbid_weekend_holding`).
- **Risk per Trade Idea** (Master): 2–3% сумарного збитку на «ідею». Ризик 1–1.6%
  на угоду не порушує; повторний вхід у той самий бік раніше 10 хв після збитку
  групується в одну ідею — стратегія в один бік двічі не входить.

Правила поза кодом (організаційні, **обов'язково виконати**):
- **EA/боти дозволені лише ВЛАСНОЇ розробки з доказом авторства** (сирці +
  git-історія + середовище розробки). Зберігай цей репозиторій з історією комітів —
  це і є доказ. Куплені/чужі боти = закриття рахунку.
- У списку заборонених практик є **«gap trading»**. Наш бот НЕ торгує ціновий геп
  інструмента (CFD котирується неперервно, входи — пробій рівня через 5+ хв після
  відкриття Xetra, ринковим ордером у ліквідний час). Але формулювання FP розмите —
  **письмово підтверди в support, що ORB-стратегія на GER40 допустима**, збережи відповідь.
- **VPS/VPN:** офіційна сторінка Trading Conduct забороняє підключення через
  VPN/VPS (джерела суперечливі — старіші згадують «дозволено з білим IP»).
  **Уточни в support.** До підтвердження — запуск лише з домашнього IP.
- Мін. торгових днів, консистентність 35% (On-Demand payout), Profit Concentration
  60% — стеж на дашборді FP; бот з ризиком ~1% на угоду їх природно не порушує.
- 30 днів без жодної закритої угоди = breach — не залишай бота вимкненим на місяць.

## 15. Параметри для оптимізації через backtest

Пріоритет за очікуваним впливом:

| Пріоритет | Параметр | Діапазон пошуку | Базлайн |
|---|---|---|---|
| 1 | `targets.fixed_rr` | 1.0 / 1.25 / 1.5 / 1.75 | 1.0 |
| 2 | `targets.tp_mode` = trailing, `trail_start_r`, `trail_step_r` | start 0.5–1.0, step 0.25–1.0 | fixed |
| 3 | `confirmation.min_body_to_range` | 0 / 0.3 / 0.5 / 0.6 | 0.5 |
| 4 | `confirmation.min_break_points` / `min_break_or_frac` | 0–10 п. / 0–0.2 | 0 |
| 5 | `entry.counter_gap_max_points` | 0 / 40 / 60 / 80 | 0 |
| 6 | `filters.max_or_width_points` | 0 / 70 / 100; або ATR-мульт 0.3–0.7 | 0 |
| 7 | `filters.min_or_rvol` | 0 / 0.8 / 1.0 / 1.2 | 0 |
| 8 | `filters.skip_weekdays` | [] / [4] / [2] | [] |
| 9 | `session.or_minutes` | 5 / 15 / 30 | 5 |
| 10 | `filters.or_direction_filter`, `vwap_filter` | on/off | off |
| 11 | `stops.sl_buffer_points` | 0–5 | 2 |
| 12 | `session.pos1_cutoff` | 10:00–12:00 Berlin | 11:00 |
| 13 | `risk.risk_per_trade_pct` (після вибору моделі, від макс. серії) | 0.8–1.6 | 1.0 |

Методологія: walk-forward (навчання 6 міс → перевірка 2 міс), метрики §16 звіту
бектесту (WinRate, PF, expectancy, max losing streak, max DD, місячний PnL).
Зміна параметра за результатами бектесту = оновлення цього файлу і config.yaml,
НЕ зміна коду.

---

## Додаток А. Відомі розбіжності першоджерел

1. «60 pips» у PDF (фільтр гепу) у транскрипті уточнено: це поріг **гепу** для
   дозволу контр-геп входу, не ширини OR (спікер сам виправився). Реалізовано як
   `entry.counter_gap_max_points`.
2. PDF: «друга позиція після відпрацювання або інвалідації першої»; транскрипт
   (базова статистика): «друга береться завжди після зворотного пробою, незалежно
   від першої». Реалізовано за транскриптом (базова модель); фільтр «пропустити
   другу, якщо перша в TP» — кандидат оптимізації.
3. Академічні дані US (EoD exit > fixed target) суперечать статистиці
   першоджерела DAX (fixed 1R/1.5R). Обидва режими реалізовані; вирішує бектест.
4. Дослідження ES/NQ не знайшло ефекту напрямку гепу, першоджерело на DAX —
   знайшло (186/54). Довіряємо DAX-специфічній статистиці; геп-фільтр вимикається
   конфігом для перевірки.

---

## Додаток Б. Валідація на даних брокера (2026-08-29)

Перша перевірка описаної вище моделі на реальних котируваннях, а не на
статистиці першоджерела. Дані: **GER40 @ FundingPips-SIM1, M5, 69 597 барів,
2025-08-22 .. 2026-08-28** (258 торгових днів). Витрати: спред 2.0 + сліпедж
1.0 пункт індексу. Рахунок 10 000 USD, ризик 1%. Звіти — у `reports/`.

### Що вийшло

| Модель | Угод | WR | PF | Expectancy | PnL | Max DD |
|---|---|---|---|---|---|---|
| Базова (без фільтрів, 1R) | 229 | 46.7% | 0.84 | −0.086R | −15.6% | 17.79% |
| Перша позиція | 107 | 48.6% | 0.89 | −0.065R | −5.3% | 10.09% |
| Друга позиція | 122 | 45.1% | 0.81 | −0.104R | −10.3% | 11.63% |

### П'ять висновків

1. **Ключове твердження першоджерела не відтворилося.** §6 будує модель на тому,
   що друга позиція — найсильніша ланка (WR 61.8%, PF 1.62 на 240 днях FDAX).
   На GER40 за цей рік друга позиція дала WR 45.1% / PF 0.81 і виявилася
   **гіршою** за першу. Це не розбіжність у деталях, а спростування основи.

2. **Проблема не в цілі (§10).** Прогін сітки `fixed_rr` = 1.0 / 1.25 / 1.5 / 1.75
   дає PF 0.84 / 0.85 / 0.84 / 0.82 — результат не реагує на розмір тейка.
   Пріоритет 1 таблиці §15 вичерпано без ефекту.

3. **Проблема не в керуванні позицією (§11).** Динамічний трейлінг
   (`trail_start_r` 0.5, `trail_step_r` 0.5 — беззбиток на +0.5R, далі підтяг
   кожні 0.5R) дав PF 0.62 і −27.8% — удвічі гірше за fixed 1R. Сітка навколо
   (step 0.25 / step 1.0 / start 1.0) — PF 0.70 / 0.61 / 0.68, теж гірше.
   Механізм видно поугодно: беззбитковий стоп зрізав **27 зі 101** виграшної
   угоди в нуль і не врятував **жодної** збиткової; верхній хвіст надто тонкий
   (15 угод за +1R, 3 за +2R, максимум +2.97R), щоб це окупити. Пріоритет 2
   таблиці §15 вичерпано.

4. **Проблема не у витратах.** При `spread_points` = 0 і `slippage_points` = 0
   модель дає WR 50.2%, PF 0.97, expectancy −0.007R — рівно беззбитковість.
   Тобто сирий сигнал при 1R статистично не відрізняється від підкидання монети;
   витрати (0.079R на угоду, ≈3 пункти при середньому 1R = 74.7 пункти) лише
   дотискають нульовий edge у мінус.

5. **Проблема не у фільтрах (§7).** Повний перебір: 7 схем виходу × 1152
   комбінації фільтрів = 8064 прогони (`tools/sweep.py`), кожен оцінений
   окремо на навчальній вибірці та на відкладеному хвості з 2026-05-01.
   Поодинці позитивні лише дві клітинки з 98, обидві на RVOL. Комбіновано
   прибуткові на всьому періоді 10.9% конфігурацій — рівно те, що дає шум при
   такій ширині пошуку. Кореляція IS→OOS: +0.171 (Пірсон), +0.079 (Спірмен);
   позитивних в обох вибірках 1.2% проти 0.7% очікуваних при незалежності.
   **Усі 10 лідерів навчальної вибірки містять `skip_weekdays=[Чт]`** —
   фільтр без апріорного обґрунтування, знайдений самим пошуком; а фільтри
   з незалежною доказовою базою в OOS від'ємні всі без винятку (найкраща
   клітинка перебору, `rvol≥1.2` при 1.5R: +0.276R → −0.526R). Деталі —
   `reports/backtest_sweep_summary.txt`. Пріоритети 6–8 і 10 таблиці §15
   вичерпано.

**Наслідок для запуску:** max DD 17.79% удвічі перевищує ліміт 10% моделі
2-Step Standard — базовий конфіг завалив би челендж, а не просто дав слабкий
результат. До live у такому вигляді стратегія не допускається.

### Чого перевірка НЕ каже

- Це один рік і 229 угод. Стандартна похибка expectancy ≈ 0.066R, тож
  «−0.086R» і «нуль» статистично розрізняються слабо. Твердження тут —
  «edge не продемонстровано», а не «доведено, що стратегія збиткова».
- Спред 2.0 заміряно на **закритому** ринку; сесійний може бути меншим.
  Перемір і перепрогони — але з §7 видно, що це не змінить знак.
- Фільтри §7 не перевірялися **свідомо**: підбір фільтрів на моделі з нульовим
  edge — це підгонка під історію, від якої застерігає той самий §7.

### Відкриті напрями

1. Довша історія (2–3 роки M5, якщо брокер віддасть) + walk-forward за §15.
2. RVOL-фільтр (§7.4) — єдиний із незалежною доказовою базою; перевіряти лише
   як окрему гіпотезу і лише out-of-sample.
3. Питання інструменту: першоджерело торгувало **ф'ючерс FDAX** без спреду;
   можливо, модель не переносить на CFD із поточними витратами — або період
   2025–2026 структурно відрізняється від періоду першоджерела.

---

## Додаток В. Ідеї з відео-джерел (2026-08-29)

Розібрано чотири відео (транскрипти витягнуті автоматично) на предмет
механічних правил, які можна перевірити нашим движком.

| Джерело | Що це насправді | Що витягнуто |
|---|---|---|
| Tom Hougaard, «Scalping the US opening minutes» | ручний скальпінг NASDAQ/FTSE на M1 у перші хвилини відкриття США; фейдить пробій попереднього бару та край овернайт-діапазону | **вимога рішучого пробою** (§5, `min_break_or_frac`) — дзеркало його фейду крайових пробоїв; плюс наголос на спреді як вирішальному чиннику |
| Tom Hougaard, «Trade Mentor — Trading Strategy» | «reverse divergence» на форексі: 89-MA як тренд + стохастик 14-3-3 | **трендовий фільтр §7.9** |
| tomtrades, «Watch Me Backtest My 71% Win Rate Strategy» | дискреційний mean-reversion на золоті: діапазон → high-volume extension у край → вхід на повернення до 50% | **обсяговий фільтр §7.10** |
| Cryptology Key, «Open Range NASDAQ» | ORB на NASDAQ, найближче до нашої моделі | **модель «ретест + поглинання»** — не реалізована, див. нижче |

### Що перевірено і з яким результатом

Повний перебір (7 схем виходу × 1944 комбінації = 13 608 прогонів,
`reports/backtest_sweep_v2_summary.txt`):

1. **§5 `min_break_or_frac` — працює.** Єдиний результат за весь проєкт, який
   тримається: монотонний за параметром, покращує **кожен** із 5 кварталів,
   навчальна й відкладена вибірки ростуть разом (при 0.20 і 1.5R: +0.039 →
   +0.204R). Але 3 з 5 кварталів усе ще від'ємні — стабільне **покращення**,
   не стабільний прибуток. Значення за замовчуванням не змінюю: 116 угод на рік
   замало, щоб фіксувати параметр.
2. **§7.9 трендовий MA — не дотягує.** MA(89) підіймає базлайн з −0.086R до
   −0.022R на 180 угодах; вимога нахилу не додає нічого.
3. **§7.10 обсяг пробійної свічки — робить гірше** (−0.16…−0.24R). Причина
   змістовна: у першоджерелі високий обсяг позначає **виснаження**, яке автор
   фейдить, а ми на цій же свічці входимо в продовження.
4. **Сітка в цілому — шум.** Кореляція IS→OOS від'ємна (−0.090). Саме тому
   пункт 1 і заслуговує довіри: він працює **поза** сіткою, сам по собі.

### Реалізована модель: «ретест + поглинання» (`strategy.name: retest`)

Джерело: Cryptology Key, «Open Range NASDAQ». Реалізовано окремою стратегією
(`strategy/retest.py`) через інтерфейс `strategy/base.py::Strategy` — движок,
розрахунок лота, трейд-менеджер і live-раннер беруть її без змін.

**Правила:**

1. OR — той самий, що й у §2 (перша свічка відкриття).
2. Свічка закривається **тілом** за межею діапазону → напрямок задано.
3. Ціна **повертається** і торкається межі діапазону → відкат.
4. Відкат **поглинається**: закриття знову за екстремумом, який був **до**
   відкату → вхід за ціною закриття цієї свічки.
5. **Стоп — за екстремумом відкату** + `retest.stop_buffer_points`. Саме це
   робить її окремою стратегією, а не фільтром до ORB: 1R становить частку
   ширини діапазону, а не всю ширину.
6. Ціль — `targets.fixed_rr` (спільна з ORB, тож сітка виходів та сама).
7. Сетап помирає за `retest.max_pullback_bars` або якщо відкат закривається
   наскрізь за протилежною межею.

> **Оновлення (Додаток Г):** цифри нижче — на даних Funding Pips. На FTMO та
> сама модель на M1 виходить у плюс навіть із реальними витратами.

**Результат** (`reports/backtest_retest_summary.txt`):

| Дані | Угод | WR | PF | Expectancy | Середній 1R |
|---|---|---|---|---|---|
| M5, повний рік | 153 | 41.8% | 0.68 | −0.194R | 60.9 п. |
| M1, 75 сесій | 38 | 50.0% | 0.92 | −0.041R | 27.3 п. |
| M1, **без витрат** | 38 | **57.9%** | **1.38** | **+0.158R** | 25.3 п. |

На M5 модель тестувати **не можна**: екстремум відкату там за 61 пункт, майже
як стоп ORB, тобто перевіряється груба імітація. На M1 стоп стає тісним, як і
задумано першоджерелом.

**Головне**: це інший **тип** провалу, ніж в ORB. В ORB edge немає взагалі —
без витрат PF 0.98, монетка. Тут без витрат PF 1.38 і WR 57.9% при 1R, тобто
edge у сигналі є, і його повністю з'їдають витрати: 3 пункти спреду й сліпеджу
це 11% від 27-пунктового R. Це рівно та проблема, якій Гоугард присвячує пів
відео, пояснюючи, чому торгує FTSE зі спредом 0.4.

**Чого це НЕ доводить.** 38 угод за 3.5 місяця; стандартна похибка expectancy
тут ≈0.16R — розміром з увесь ефект. Розбиття на in/out-of-sample неможливе.
Порядок дій, перш ніж цьому вірити:

1. заміряти **реальний сесійний спред** GER40 при відкритому ринку;
2. дістати більше M1-історії — цей брокер віддає лише 100 000 барів;
3. лише тоді — walk-forward.

---

## Додаток Г. Перевірка на другому брокері — FTMO-Demo (2026-08-29)

Ті самі моделі прогнані на котируваннях FTMO (`GER40.cash`, contract size 1.0
проти 25.0 у Funding Pips) за той самий період. Конфіг — `config/config.ftmo.yaml`,
окремий кеш `data/cache_ftmo`, секрети — в `.env.ftmo` (у git не потрапляє).
Спреди тепер **виміряні**, а не припущені: MT5 пише спред у кожен бар,
і за 75 000 барів у вікні входу 09:05-11:00 вийшло **1.92 у Funding Pips** і **1.25 у FTMO**
(`reports/spread_measurement.txt`). Сліпедж 1.0 лишається припущенням — його
з історії барів не дістанеш. Поправка спреду не змінила жодного висновку
нижче: розбіжність між брокерами сидить у самих котируваннях, а не у витратах. Повні таблиці — `reports/backtest_ftmo_comparison.txt`.

### 1. ORB підтверджено як безнадійний — на обох брокерах однаково

Дев'ять схем виходу (fixed 1/1.5/2R + шість варіацій трейлінгу) дають майже
однакові числа й **однаковий порядок**: fixed 1R найкращий (PF 0.87 проти 0.84),
кожна трейлінгова гірша, і тим гірша, чим пізніший старт та більший крок.
Провал — властивість моделі, а не одного фіду даних.

### 2. `min_break_or_frac` — попередній висновок був завищений

| frac (1.5R) | Funding Pips | FTMO |
|---|---|---|
| 0.00 | −0.093R | −0.102R |
| 0.20 | **+0.088R** | −0.030R |
| 0.30 | **+0.131R** | +0.025R |
| 0.40 | **+0.184R** | +0.026R |

Обидва фіди згодні щодо **напрямку**: вимога рішучого пробою покращує базлайн.
Але величина відрізняється всемеро, і на FTMO відкладена вибірка йде в мінус.
У Додатку В цей фільтр названо «єдиним результатом, який тримається» — це
формулювання **спростовано**: ефект уміщається в шум, який дає сама лише зміна
постачальника даних. Напрямок лишається вартим уваги, величина — ні.

### 3. Модель ретесту зміну брокера пережила

M1, 75 сесій, вікно відкату 6 барів, fixed 1R:

| Витрати | Funding Pips | FTMO |
|---|---|---|
| реальні (2.0 + 1.0) | PF 0.92, −0.041R | **PF 1.06, +0.033R** |
| спред 1.0 + сліпедж 0.5 | PF 0.96, −0.020R | PF 1.22, +0.101R |
| нульові | PF 1.38, +0.158R | PF 1.55, +0.217R |

Обидва фіди згодні, що edge у сирому сигналі є, і що несе його **коротке** вікно
відкату: 3–6 барів працює (на FTMO при 3 барах PF 1.27, +0.124R), 10–15 — ні.
Це збігається з «ідеально в межах 3 хвилин» у першоджерелі.

### Чого два брокери НЕ доводять

Це **не дві незалежні вибірки**. Той самий індекс, ті самі дні, ті самі угоди —
різні лише котирування. Збіг виключає артефакт одного фіду і більше нічого: про
інший період він не каже нічого, а 22–41 угода лишається мізерною вибіркою.
Пункт 2 — якраз міра того, наскільки сама лише зміна вендора здатна зрушити
виміряний ефект.

---

## Додаток Д. Холдаут-перевірка і закриття ORB (2026-08-29)

Після зміни «Max bars in chart» у терміналах M5 поглибшав із 69 597 до
100 136 / 100 762 барів — близько 18 місяців. **M1 не зрушив**: обидва брокери
віддають рівно 100 000 барів і на запит 2024–2025 років повертають один бар.
Ліміт серверний, не термінальний.

Поглиблення дало те, що цінніше за обсяг: зріз **2025-03-16 … 2025-08-21**
жоден тест, перебір чи підбір параметрів у цьому проєкті не бачив. Чистий
холдаут.

### `min_break_or_frac` холдаут не пройшов

Funding Pips, fixed 1.5R:

| frac | Холдаут (не бачили) | Тюнінговий період |
|---|---|---|
| 0.00 | +0.009R (n=80) | −0.099R (n=228) |
| 0.20 | **−0.092R** (n=40) | **+0.088R** (n=116) |
| 0.30 | **−0.132R** (n=26) | **+0.131R** (n=80) |
| 0.40 | +0.047R (n=17) | +0.185R (n=60) |

Два значення, які виглядали найкращими там, де відбувався підбір, — **два
найгірші** на даних, яких вони не бачили. Ранжування не слабшає, а
**інвертується**: це підпис параметра, підігнаного під шум. Брокери ще й не
згодні, яке значення краще на тому самому холдауті (FP 0.15, FTMO 0.20).

Але вирішальний рядок — базлайн: **без жодного фільтра** ORB на холдауті
плюсовий (+0.009R і +0.028R), а на тюнінговому періоді від'ємний. Ті п'ять
місяців просто були прихильнішими до пробоїв відкриття. Те, що виглядало як
знахідка про фільтр, було **періодом**, а фільтр його зчитував.

### ORB за 18 місяців — жодна конфігурація не плюсова

Сім схем виходу × два брокери, 308 і 322 угоди. Найкраща клітинка — FTMO
fixed 1R, −0.030R ± 0.055 — у межах однієї стандартної похибки від нуля.

**Висновок: ORB закритий.** Два брокери, 18 місяців, усі схеми виходу,
21 672 комбінації фільтрів і чистий холдаут наприкінці. Подальша робота з
параметрами цієї моделі — не дослідження, а декорація. Розділи §1–§15
лишаються як специфікація того, що було перевірено, а не як план торгівлі.

### Що лишилось живим

Тільки модель ретесту на M1 (Додаток В). Її 95% довірчий інтервал
(−0.290R … +0.551R) містить нуль, і цими двома брокерами питання не
закривається: 24–41 угода — це все, що вони можуть дати. Потрібна M1-історія
ззовні.

---

## Додаток Е. Dukascopy: закриття останнього кандидата (2026-08-29)

Проп-брокери віддають максимум 100 000 M1-барів. Dukascopy віддав
**1 225 546 барів за 943 сесії** (DEUIDXEUR, 2023-01-02 … 2026-08-28) — у 12
разів більше. Власний спред Dukascopy у вікні входу — 1.51 пункту, тобто три
незалежні виміри узгоджені: FTMO 1.25, Dukascopy 1.51, Funding Pips 1.92.

### Модель ретесту на достатній вибірці — значуще збиткова

Витрати: спред 1.25 (виміряний у FTMO, де ми б торгували) + сліпедж 1.0.

| Вікно відкату | Угод | WR | PF | Expectancy | 95% інтервал |
|---|---|---|---|---|---|
| 3 бари | 253 | 45.5% | 0.76 | −0.147R | −0.276 … −0.018 |
| 6 барів | 445 | 46.7% | 0.80 | −0.114R | −0.211 … −0.017 |
| 10 барів | 581 | 47.5% | 0.84 | −0.093R | −0.178 … −0.008 |

**Усі інтервали цілком нижче нуля.** Кожен рік від'ємний: 2023 −0.050R,
2024 −0.267R, 2025 −0.044R, 2026 −0.067R.

### Edge у сирому сигналі теж відсутній

Без витрат на 445 угодах: WR 51.0%, **PF 1.04**, +0.020R ± 0.047. На 38–41
угоді було PF 1.38/1.55 і висновок «edge є, його з'їдають витрати» — на повній
вибірці це монетка. Висновок Додатка В про наявність edge **спростовано**.

### Анатомія хибного плюса

Те саме вікно 2026-05-18…08-28, яке єдине могли дати брокери:

- дані FTMO, 41 угода: PF 1.08, **+0.039R**
- дані Dukascopy, 38 угод: PF 0.61, **−0.251R**

Та сама модель, ті самі 75 сесій, той самий ринок, два фіди — **протилежні
знаки**. Вибірка не мала ані знака, ані величини.

### Підсумок проєкту

Обидва кандидати закриті:

- **ORB** — 18 місяців, два брокери, усі схеми виходу, 21 672 комбінації
  фільтрів, чистий холдаут. Edge немає.
- **Ретест на M1** — 943 сесії, 445 угод, кожен рік від'ємний, edge немає
  навіть при нульових витратах. Значуще збиткова.

Прибуткової стратегії в цьому репозиторії немає, і це тепер **вимір**, а не
брак доказів. Інфраструктура відпрацювала як належить: вона двічі згенерувала
хибний позитив на малих вибірках і обидва рази вбила його, щойно отримала
достатньо даних.

---

## Додаток Ж. Імбаланс, свіп овернайт-діапазону, три моделі поруч (2026-08-29)

Матеріали: скріншоти TradingView у `grafik-creen/` (бокси овернайт-діапазону на
GER40 M5) і книга «Smart Money Concept» (BTR Trading, 402 слайди, скан без
текстового шару). Книга — стандартний курс SMC/Вайкоффа з великою часткою
психології; механічно визначені в ній лише **імбаланс (FVG)** і **закриття
тілом**, тобто рівно те, що ти й назвав. Їх і реалізовано.

### Що додано

- **§7.11 фільтр імбалансу** (`strategy/imbalance.py`, `filters.require_fvg`) —
  тribar-гап, де сусіди імпульсної свічки не перекриваються. Підключений до
  ORB і до ретесту.
- **Стратегія `sweep`** (`strategy/sweep.py`) — діапазон, набраний **до**
  відкриття касової сесії, пробивається, ціна закривається назад усередину, і
  вхід іде **проти** свіпу зі стопом за його екстремумом. Це перша модель тут,
  яка фейдить: обидві попередні — продовження, і обидві виміряні як безедж'ові,
  тоді як усі розібрані дискреційні джерела торгують саме проти руху.

### Результат: 11 варіантів × 9 схем виходу = 99 прогонів, 943 сесії

- позитивних клітинок — **5 із 99**, усі в діапазоні +0.002…+0.011R;
- клітинок, чий 95% інтервал вище нуля, — **жодної**;
- значуще від'ємних — **52**;
- **свіп — найгірша з трьох моделей**: −0.11…−0.28R на кожній схемі виходу.

Без витрат позитивних 53 зі 99, але **знову жодного інтервалу вище нуля**;
найкраща клітинка +0.148R ± 0.121.

### Головний висновок — про структуру витрат

Середня вартість угоди в R за моделями:

| Модель | Типовий стоп | Витрати |
|---|---|---|
| ORB | ~75 пунктів | 0.067R |
| Свіп M5 | ~40 пунктів | 0.115R |
| Ретест / свіп M1 | ~25–30 пунктів | 0.157–0.180R |

Кожне звуження стопа, яке робить модель привабливою на папері, **множить те, що
забирає спред**. Моделі на 27-пунктових стопах треба мати понад 0.16R edge, щоб
просто вийти в нуль — а 0.16R це приблизно найбільший сирий ефект, який тут
узагалі з'явився, причому з похибкою, більшою за сам ефект.

### Підсумок

Продовження провалюється (ORB, ретест). Фейд провалюється теж і сильніше (свіп).
Фільтр імбалансу зсуває клітинки на соті R в обидва боки — так поводиться
фільтр, коли фільтрувати нічого.

Три родини моделей, дев'ять схем виходу, три джерела даних, чотири роки.
Діапазон відкриття DAX не несе торгованого edge в жодній із перевірених форм —
ні як пробій, ні як вхід на відкаті, ні як фейд.

