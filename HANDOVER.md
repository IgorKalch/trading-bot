# Стан роботи на 2026-08-30

Нотатка для продовження. Повні деталі — у [STRATEGY.md](STRATEGY.md) (Додатки А–Ж)
та [reports/README.md](reports/README.md) (журнал усіх прогонів).

## Що встановлено

**Закрито остаточно (вимір, а не брак доказів):**

- **ORB на DAX** — 18 місяців, два брокери, усі схеми виходу, 21 672 комбінації
  фільтрів, чистий холдаут. Edge немає (Додаток Д).
- **Ретест на DAX M1** — 943 сесії Dukascopy, 445 угод, −0.114R ± 0.050,
  95% інтервал цілком нижче нуля, кожен рік від'ємний (Додаток Е).
- **Свіп овернайт-діапазону на DAX** — найгірша з трьох, −0.11…−0.28R (Додаток Ж).
- **`min_break_or_frac`** — провалив холдаут із інверсією рангів; на NQ активно
  шкодить. Це був шум.

**Живий кандидат — ретест на NQ:**

| | Значення |
|---|---|
| Конфіг | `strategy.name: retest`, M1, `max_pullback_bars: 6`, `fixed_rr: 1.0` |
| Дані | Dukascopy NDXUSD, 2023-01..2026-08, 952 сесії |
| Витрати | ф'ючерсні: спред 0.25 + сліпедж 0.25 |
| Результат | n=428, WR 55.1%, PF 1.21, **+0.096R ± 0.049** |
| IS → OOS | +0.110 → +0.076 |
| По роках | +0.196 / +0.045 / +0.011 / +0.132 — усі плюсові |
| Нуль-модель | −0.028R; реальна на **4.6 σ вище** |
| Просадка | 12.6R (при 0.5% ризику = 6.3%, ≈5.5%/рік) |

Причина, чому саме NQ: **витрати відносно R**. NQ 1.4%, DAX 4.5%, ES 6.1% —
і expectancy монотонна за цим числом.

**Застереження:** перевірено 594 клітинки, випадково очікується ~30 хибних
позитивів; доказом є перевірки вище, а не інтервал (його нижня межа +0.001).
NDXUSD — це CFD, проксі до ф'ючерсу NQ.

## Фінальна конфігурація (2026-08-30)

`strategy.name: retest`, NQ (Dukascopy NDXUSD) **M1**, сесія **09:30 America/New_York**,
OR **5 хв**, `max_pullback_bars: 6`, `fixed_rr: 1.0`,
`filters.require_fvg: true`, `filters.trend_ma_period: 89`,
витрати: спред 0.25 + сліпедж 0.25.

349 угод / 44 місяці: WR 57.6%, PF 1.34, **+0.146R**, просадка 10.3R.
IS +0.137 → OOS +0.158. Усі роки плюсові. 4.6 σ над нуль-моделлю.
Блоковий бутстреп по кварталах: 95% інтервал +0.059…+0.233, нижче нуля 0.03%.

**Гроші:** 0.58%/міс при ризику 0.5% (просадка 5.1%); 61% місяців плюсові,
найгірший −2.5%. До цілі челенджу 10% — 12–17 місяців.

**Пошук вичерпано.** Не працює: інші сесійні якорі на NQ, інші довжини OR,
друга модель на тому ж інструменті, портфелі з кількох моделей, шість інших
індексів (єдина причина — cost/R: NQ 1.4%, решта 3–24%).

**За місяць челендж пройти неможливо** — потрібен ризик 8.6% на угоду, стеля
ймовірності 57% при 43% зливу, а денний ліміт 5% робить такий ризик недосяжним.

## Наступні кроки за пріоритетом

1. **`signal`-режим на demo** — заміряти реальний сліпедж. Це єдина неперевірена
   цифра в моделі, і вона безкоштовна. Усе інше з даних уже витиснуто.
2. **Справжні дані ф'ючерсу NQ** (не CFD) — FirstRate Data, разова покупка,
   19 років M1. Наразі задорого; коли з'явиться — вибірка виросте вчетверо.
2. **Forward-тест** у `signal`-режимі, щоб заміряти реальний сліпедж
   (`slippage_points: 1.0` досі припущення, з історії барів не вимірюється).
3. Тільки після цього — гроші, і не більше 0.5% ризику на угоду.

## Правило для довгих воркфлоу

Цей воркфлоу двічі помирав на ліміті токенів, не дійшовши до синтезу. Тепер
правило таке — і воно **вбудоване в сам скрипт**, а не лише записане тут:

1. **Ніколи не доводити до вичерпання ліміту.** При **95% використання** —
   зупинити відкриття нових агентів, зробити бекап і завершитися.
2. **Завжди видавати результат.** Фаза синтезу тепер працює на тому, що вже
   перевірено, а не чекає на повний прохід. Часткова відповідь краща за
   відсутню.
3. **Не мовчати про обрізане.** Скрипт логує, скільки кандидатів перевірено з
   загальної кількості й чому решту відкинуто.
4. **Нічого не втрачається в будь-якому разі.** Кожен завершений агент лежить у
   `journal.jsonl` і при відновленні повертається з кешу миттєво.

Технічно: у скрипті стоїть `VERIFY_CAP = 24` і перевірка
`budget.spent() >= budget.total * 0.95`. Якщо ліміт токенів на сесію заданий
(директивою на кшталт «+500k»), спрацює друга умова; якщо ні — перша.

**Що робити мені, коли користувач каже про залишок ліміту:** негайно зупинити
воркфлоу, дописати нотатки сюди, закомітити й запушити. Не починати нових
прогонів.

## Незавершене №2: дослідження OTD/ORR + Initial Balance (2026-09-01)

Зупинено на прохання користувача о 21:10, **194 записи журналу, 105 агентів**
уже в кеші. Фази пошуку і переведення в механічні правила пройдені; зупинка
сталася на адверсарній верифікації.

Перезапустити:

```
Workflow({
  scriptPath: "C:\Users\i.kalchenko\.claude\projects\c--Users-i-kalchenko-source-repos-IK-trading-bot\0469be3f-2d1c-45ed-86c3-16897f67800c\workflows\scripts\research-open-types-ib-wf_ebab128a-b71.js",
  resumeFromRunId: "wf_ebab128a-b71"
})
```

Що він має дати: механічні визначення чотирьох типів відкриття Далтона
(OD / OTD / ORR / OA) з числовими порогами на закритих 5-хвилинках, визначення
входу «5m BOS», нуль-гіпотезу для перевірки, і три найімовірніші способи, якими
сетап обманює своїх авторів.

**Це потрібно, щоб перевірити другу половину сетапу** — 85 угод із заявленими
76.4% WR при 1:1. Перша половина (IB extension) уже виміряна:
`reports/backtest_ib_open_types.txt`.

### Що вже з'ясовано без воркфлоу

- **IB розширюється у 96.7% днів на NQ і 97.2% на DAX** — передумова сетапу
  майже завжди істинна, тобто не несе інформації.
- Проксі reversal-типу відкриття зсуває це лише на 1.6 пункти.
- **Ширина IB — єдина умовна змінна, що працює**, і протилежно до фольклору:
  широка перша година вдвічі зменшує шанс двобічного дня (13.4% проти 28.9%).
- Механічний IB extension на DAX: **51.6% WR проти заявлених 72%**. Розрив
  пояснює фраза самих авторів — «гнучка постановка SL залежно від контексту».
- NQ, IB(60), ширина ≥0.9% ціни: 83 угоди, PF 1.53, +0.100R, просадка **2.0R**,
  усі 4 роки плюсові. Річний MAR 1.13 проти 1.35 у ретесту, але вп'ятеро рідше.
  **Перша нога за весь проєкт, яка має власний edge** — варта перевірки на
  кореляцію з ретестом.

## Незавершене: воркфлоу пошуку стратегій

Зупинявся тричі через ліміт токенів (2026-08-29 та двічі 2026-08-30). Робота
НЕ втрачається: журнал уже містить **418 записів і 233 агенти**, і всі вони
повертаються з кешу миттєво. Фаза пошуку (8 напрямів) і фаза переведення в
механічні правила завершені; зупинка щоразу траплялася на фазі адверсарної
верифікації.

Перезапустити тією самою командою — вона підхопить кеш:

```
Workflow({
  scriptPath: "C:\\Users\\i.kalchenko\\.claude\\projects\\c--Users-i-kalchenko-source-repos-IK-trading-bot\\0469be3f-2d1c-45ed-86c3-16897f67800c\\workflows\\scripts\\hunt-tradeable-strategies-wf_70acb0f8-ab6.js",
  resumeFromRunId: "wf_70acb0f8-ab6"
})
```

Що він робить: вісім паралельних напрямів пошуку (академічні праці/SSRN,
intraday momentum, квант-блоги, Quantpedia, GitHub, докази ЗА і ПРОТИ ORB,
література провалів, структура витрат по інструментах), потім переведення
кожного кандидата в механічні правила, потім три адверсарні рецензенти на
кандидата (докази / однозначність / виживання під витратами), потім критик
повноти.

Фінальний промпт критика **оновлено 2026-08-30** — він тепер отримує всі наші
виміри (cost/R по семи ринках, фінальну конфігурацію NQ, вичерпаний пошук,
математику челенджу) і має відповісти на п'ять питань, зокрема: який кандидат
дав би **більше за 1.16R на місяць** при просадці до 10R, і чи існує клас
стратегій з іншим профілем дохідності (мультиденні, овернайт-ефекти,
календарні, міжринкові), бо внутрішньоденний скальпінг індексів вичерпано.

## Інфраструктура

- `tools/grid.py` — стратегія × вихід × фільтр на одному наборі даних;
  99 клітинок за 50 с, на кожній стандартна похибка. Приймає `--spread`,
  `--slippage`, `--config`.
- `tools/sweep.py` — повний перебір фільтрів для однієї стратегії
  (13 608 прогонів за 280 с), з розбиттям на навчальну та відкладену вибірки.
- Конфіги: `config.yaml` (Funding Pips), `config.ftmo.yaml`, `config.duka.yaml`
  (DAX), `config.es.yaml`, `config.nq.yaml`. Секрети — у `.env*`, поза git.
- Дані Dukascopy тягнуться скриптом із `pip install dukascopy-python`;
  проп-брокери віддають максимум 100 000 M1-барів, Dukascopy — мільйони.
- Виміряні спреди у вікні входу: FTMO 1.25, Dukascopy DAX 1.51,
  Funding Pips 1.92, ES 0.50, NQ 1.34. **Середнє за добу — пастка** (DAX 3.91).

78 тестів, `ruff` чистий, усе запушено в `main`.

## Дослідження Market Profile / типів відкриття Далтона (2026-09-02)

### Встановлено остаточно

**Типи відкриття Далтона не несуть інформації.** Механічні OD/OTD/ORR/OA на
5-хвилинках із порогами в частках 14-денного діапазону: найкращий вінрейт на
семи ринках — 55.6% проти заявлених 76.4%. ORR трапляється на 21 сесії з 906.
Тип OA, який велено **пропускати**, працює не гірше за «торгові». Видалення
класифікації повністю обіграє власний вибір джерела.
→ `reports/backtest_markets_and_dalton.txt`

**Сімейство фільтрів Market Profile не працює.** Десять гіпотез, заданих
**заздалегідь**, на трьох живих моделях = 30 клітинок. У 29 із 30 частка
викинутих **збитків** дорівнює частці викинутих **угод** (67/66, 39/38, 46/47,
33/33, 64/64, 83/84, 37/35, 84/84, 45/44, 39/39) — фільтри не розділяють
переможців від переможених, лише зменшують вибірку. На drive-моделі **кожен**
фільтр знижує MAR.
→ `reports/backtest_profile_filters.txt`

**Єдина зачіпка провалила dose-response.** Confluence + діапазон учора < 1.0 ATR
був єдиною клітинкою, де відкинуті угоди справді втрачали. Квінтилі:
+0.068, +0.144, +0.022, +0.157, −0.004 — другий і четвертий добрі, решта ні.
Залежності від волатильності немає; поріг просто впав туди, де відділив щасливу
пару кошиків від нещасливої трійки. 15 комбінацій змінна×модель, rho від −0.60
до +0.60 — чистий шум.
→ `tools/volregime.py`

### Інфраструктура, готова до використання

- `tools/mprofile.py` — справжній TPO-профіль із M1: POC, зона вартості, VAH,
  VAL, single prints, poor high/low, naked POC, balance/trend день. Висота біна
  = 14-денний діапазон / 120, **ніколи** фіксовані пункти.
- `tools/markets.py` — 5 моделей × 7 ринків, витрати ф'ючерсні й CFD окремо.
- `tools/filters.py` — фільтри з колонками `%tr` проти `%loss` і MAR. Саме ці
  дві колонки вирішують долю фільтра; expectancy сама по собі нічого не значить.
- `tools/portfolio.py`, `tools/validate_drive.py`, `tools/volregime.py`.

### Два воркфлоу в роботі

Відновлювати з кешу (завершені агенти повертаються миттєво):

```
Workflow({scriptPath: "C:\Users\i.kalchenko\.claude\projects\C--Users-i-kalchenko-source-repos-IK-trading-bot\0469be3f-2d1c-45ed-86c3-16897f67800c\workflows\scripts\market-profile-hunt-wf_d9559ae5-21e.js", resumeFromRunId: "wf_d9559ae5-21e"})
Workflow({scriptPath: "C:\Users\i.kalchenko\.claude\projects\C--Users-i-kalchenko-source-repos-IK-trading-bot\0469be3f-2d1c-45ed-86c3-16897f67800c\workflows\scripts\video-reanalysis-wf_201a3015-a66.js", resumeFromRunId: "wf_201a3015-a66"})
```

Перший — 12 напрямів пошуку в інтернеті (книги Далтона, статті, блоги, YouTube,
форуми, докази ПРОТИ), потім переведення в механічні правила, потім три
адверсарні рецензенти на кандидата, потім критик повноти.

Другий — повторний аналіз `TO_IB/iborrotd.mp4` (76 хв): 8 часових зрізів +
6 тематичних лінз, потім те саме конвеєром.

### Артефакти відео (у scratchpad сесії, не в git)

- транскрипт: `iborrotd_transcript.txt` — 1818 рядків із таймкодами,
  faster-whisper `small`, російська;
- кадри: `frames/f_0001.jpg`..`f_0305.jpg`, **кадр N = (N−1)×15 секунд**;
- відтворити: `pip install imageio-ffmpeg faster-whisper`, далі
  `scratchpad/transcribe.py`.

### Заплановане пробудження

Одноразовий cron на **02:00** (`2 2 3 9 *`, job `708536d8`) з інструкцією
відновити обидва воркфлоу з кешу й довести до звіту. Живе лише в цій сесії.
